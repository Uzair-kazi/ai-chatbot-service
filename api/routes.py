"""
API Routes

This module defines all API endpoints for the Admin AI Chatbot.
Routes are organized under the /v1 prefix for versioning.
"""

from fastapi import APIRouter

# Create API router
router = APIRouter()

# Endpoints will be added in subsequent units
# - POST /v1/ask - Main chatbot endpoint (Unit 3)
# - GET /v1/health - Health check endpoint (Unit 7)
# - GET /v1/metrics - Metrics endpoint (Unit 8)
